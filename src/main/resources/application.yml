spring:
  application:
    name: picpay-desafio
  datasource:
    url: jdbc:h2:file:./data/picpay
    driverClassName: org.h2.Driver
    username: sa
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  h2:
    console:
      enabled: true
      path: /h2-console
  sql:
    init:
      mode: always